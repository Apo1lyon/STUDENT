---
tags:
  - Практика
  - ЯзыкПрограммирования
  - Python
---
- Создайте базовый класс `Animal` с атрибутами `name` и `age`.
- Создайте подклассы `Dog`, `Cat`, `Bird`, наследующие от `Animal`.
- Добавьте в каждый подкласс специфические методы (например, `bark()` для `Dog`, `meow()` для `Cat`).
- Создайте несколько объектов разных классов и продемонстрируйте использование их методов.